import SwiftUI

struct CohortAnalysisView: View {
    var body: some View {
        ContentUnavailableView(
            "Cohort Analysis",
            systemImage: "person.3",
            description: Text("Multi-sample/cohort analysis will live here. (Placeholder)")
        )
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

